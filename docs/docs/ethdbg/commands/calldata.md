Print the calldata sent to the current executing contract.
A specific portion of the calldata can be printed by specifying the `<offset>` and the `<length>`

Usage: `calldata <offset> <length>`

![](../../imgs/calldata.png)